name: mobboards
description: A MobBoards Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.2
  package_info: ^2.0.2 # ^0.4.0+16
  responsive_builder: ^0.4.1
  flutter_tts: ^3.2.2 #^3.0.0
  device_preview: ^0.7.4 # ^0.7.1
  get: ^4.3.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - i18n/en.json
    - i18n/es.json
    - i18n/pt.json
    - assets/